apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pintu-sre-helm.fullname" . }}-test-connection"
  labels:
    {{- include "pintu-sre-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pintu-sre-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
