apiVersion: v1
kind: Service
metadata:
  name: {{ include "pintu-sre-helm.fullname" . }}-golang
  labels:
    {{- include "pintu-sre-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pintu-sre-helm.selectorLabels" . | nindent 4 }}